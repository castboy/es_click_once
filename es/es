#!/bin/bash

function start() {
	echo $$ > ${APT_PID_FILE}/${App}.pid
	su - es -c "exec ${Apthome}/package/${App}/elasticsearch-5.2.2/bin/elasticsearch"
	exit
}

function stop() {
	kill `cat /var/run/${App}.pid`	
}

Apthome=${APT_HOME}
App="es"
case $1 in
  "start")
  start
  ;;
  "stop")
  stop
  ;;
  "restart")
  ;;
esac

exit 0